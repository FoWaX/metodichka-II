#include <iostream>
#include <set>
#include <string>
#include <vector>
using namespace std;

int main()
{
	
	vector<string> st;
	set <char> st1;
	int n;
	cin >> n;
	cin.get();
	for (int i = 0; i < n; i++)
	{
		string x;
		getline(cin, x);
		st.push_back(x);
	}
	vector <string> ::iterator it = st.begin();
	vector <string> ::iterator ig = st.end();
	ig--;
	for (; it != ig; it++)
	{
		string s;
		s = *it;
		for (int i = 0; i < s.size(); i++)
		{
			if (!(isalnum(s[i]) || s[i] == ' ')) 
			st1.insert(s[i]);
		}
	}
	string s = *(st.rbegin());
	set <char> ::iterator it1 = st1.begin();
	for (; it1 != st1.end(); it1++)
	{
		char z;
		z = *it1;
		if (s.find(z) == string::npos)
		{
			cout << z << ' ';
		}
	}
	return 0;
}
